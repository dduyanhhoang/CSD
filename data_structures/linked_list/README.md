Linked list:

```
+head
+size

-insert_first(value)
-insert_last(value)
-insert_after_node(node, value)
-delete_first()
-delete_last()
-delete_after_node(node)
-update(target_node, value)
-search(value)
-__str__()
```
