# Array

```
Array structure:
+array
+index
+capacity

-insert(value)
-delete(index)
-update(index, value)
    check -> delete -> insert?
-search(value) -> bool
-display()
```

# Dynamic Array

```
Dynamic array structure:
+elements
+capacity
+array
-DynamicArray
-resize
-append
-insert(index)
-remove(value)
-remove(index)
```