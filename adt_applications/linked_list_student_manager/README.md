Student:

```
+id unique
+name
+mark

-insert_first(Student)
-insert_last(Student)
-delete_fist()
-delete_last()
-delete_by_id(id)
-search_by_name(name)
-search_by_id(id)
-update(id)
-display_students()
```
